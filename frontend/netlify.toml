[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20.19.0"

# Simple SPA redirect - Netlify serves static files automatically
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200