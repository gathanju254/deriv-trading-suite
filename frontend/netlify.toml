[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.19.0"

# SPA fallback — DO NOT TOUCH ASSETS
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200