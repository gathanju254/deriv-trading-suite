[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20.19.0"

# SPA redirect - ONLY for HTML routes, not assets
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  # Add condition to exclude assets
  conditions = {
    Role = ["role=404"]
  }