[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20.19.0"

# SPA redirect - catch all routes and serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200